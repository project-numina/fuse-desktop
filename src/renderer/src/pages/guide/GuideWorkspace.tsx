import GuidePageLayout from '@/components/guide/GuidePageLayout';
import GuideWorkspace from '@/components/guide/GuideWorkspace';

export default function GuideWorkspacePage() {
  return (
    <GuidePageLayout slug="workspace">
      <GuideWorkspace />
    </GuidePageLayout>
  );
}
