# Keep this app installable on its own: it is not a member of the monorepo
# pnpm workspace and ships its own package-lock.json.
package-lock=true
# npm 10.9 crashes resolving jsdom's optional canvas peer without this.
legacy-peer-deps=true
